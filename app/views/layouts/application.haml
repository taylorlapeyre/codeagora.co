!!!
%html
  %head
    %title Agora
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    - if signed_in?
      %h1 Signed in as #{current_user.username}

    - flash.each do |key, value|
      %div{:class => "alert alert-#{key}"}= value

    = yield